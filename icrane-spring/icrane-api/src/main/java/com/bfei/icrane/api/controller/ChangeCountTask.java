package com.bfei.icrane.api.controller;

//@Component("taskJob")
public class ChangeCountTask {
  /* RedisUtil redisUtil = new RedisUtil();
    @Autowired
    ChargeService chargeService;
    //@Autowired
    DollOrderService dollOrderService;
    private static String CACHE_LOCK = "cache_lock";  
    // *5 * * * * ? 五秒一次
    // 0 0 0 * * ? 每天0点
   // @Scheduled(cron = "0 0 0 * * ?")
    public void job1() {
    	if (redisUtil.getString(CACHE_LOCK) == null) {  
    		redisUtil.setString(CACHE_LOCK, "TRUE",3600);  
    		List<DollOrder> dollOrderList = dollOrderService.selectExpireOrder();
    		chargeService.changeCount(dollOrderList);
        }
        
    }
    */
}